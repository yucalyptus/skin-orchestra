.DS_Store
*.tmp
Thumbs.db
img/

# Excelの一時ロックファイル
~$*

# Pythonのビルド副産物
__pycache__/
*.pyc

# Codex等の出力先（成果物は 資料/ へコピーして管理する）
outputs/

# プレビュー（著者チェック用。公開しない）
_preview/

# Pillow を入れた作業用の仮想環境（各自のマシンで作る）
.venv/

# 図のソースPNG（554MB）。いま作り直し中なので、まだ履歴に入れない。
# Gitはバイナリを差分で持てないため、旧版を先に入れるとリポジトリが永久に重くなる。
# 作り直しが終わったら、この2行を消して1回でコミットする（旧版は履歴に残らない）。
figures/

# Claude Code の各自のローカル設定
.claude/settings.local.json
