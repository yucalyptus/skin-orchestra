# 作業メモ

**いま何をしているか・保留中のタスクだけを書く。**決定事項と全体像・段取り・検証結果は `資料/移行プラン.md`。

---

## いまここ（2026-08-17）

旧プロジェクトから移した本文・図を、章番号を捨てた新設計へ移す作業。
**移行の機械作業とビルドの作り直しは完了**（内訳は `資料/移行プラン.md`「段取り」）。いまは**著者チェック**の段階。

### 次にやること

1. **著者チェック** ← いまここ。`_preview/index.html` を読み、OKな章を `status: approved` にして `published:` と `history:` を入れる
   - どの章がどの status かは `資料/章一覧.md`（自動生成）を見る
   - 公開済みの章は**ダブルチェック中**。表現の引っかかりを直しながら進めている
2. **図2点**が未作成 … `transdermal-delivery_越えさせ方の違い` / `transdermal-delivery_まとめ`
3. **腸内細菌**は制作メモだけ。骨と構成は本文中のコメントに残してある
4. `figures/`（ソースPNG）は当面 `.gitignore`。**図の差し替えが一段落したら1回で入れる**（旧版を履歴に残さないため）

**後回し**：主訴索引（front matter `適応:` ＋索引ページ）。分類軸を決めるのが先。

---

## 保留タスク

- **図が0点の章が1つある** … `抗酸化サプリメントと点滴`。`資料/図版TODO.md` へ起こす
- **文献の照合** … `資料/校正TODO.md`。確度が低いものに固有番号を付けない方針は維持
- **著者レビューの反映** … `資料/著者レビュー_2026-08.xlsx`
- **図版TODO** … `資料/図版TODO.md`
- **平易版（看護師版）** … `資料/平易版_仕様.md`。新サイトに含めるかは未決定

**決めていないこと**は `資料/移行プラン.md`「まだ決めていないこと」に集約。
