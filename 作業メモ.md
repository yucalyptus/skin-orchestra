# 作業メモ

**いま何をしているか・保留中のタスクだけを書く。**決定事項と全体像は `資料/移行プラン.md`。

---

## いまここ（2026-08-17）

旧プロジェクトから移した本文57章・図326点を、**章番号を捨てた新設計へ移す作業**。
**移行の機械作業は完了。** いまは著者チェックの段階。

### 済んだこと

- 本文・図・資料のコピー（旧は無傷）
- **構成を決めた**（4セクション11部。詳細は `資料/移行プラン.md`）
- `目次.yml` を2階層（section > part > 章）に書き換え、部の `intro` を付与
- **本文57章の変換がすべて完了**
  - front matter 付与／本文のH1削除
  - ファイル名から章番号を落とす
  - 図325点を `<id>_図名.png` にリネーム＋本文の画像参照325件
  - `第N章` 855件・並列33件・範囲11件・`第N部` 50件 → `[[id]]` ／ 部名
  - 並び替えで壊れた送り文44件の書き直し、**順序語（次章・前章）を全廃**
  - 部の継ぎ目宣言14件を本文から抜き、`目次.yml` の部 `intro` へ移設
- `CLAUDE.md`・`README.md`・`資料/移行プラン.md` に決定事項を反映
- `資料/章一覧.md` を新設（自動生成）

### 検証（毎回ここを見る）

| | |
|---|---|
| 目次 ↔ md | 一致・過不足なし |
| `[[id]]` 参照 | 解決できない参照 **0**（あればビルドが止まる） |
| 図 | 参照と実体が一致。欠けは「導入」の2点のみ |
| `第N章` / `第N部` / 「次章・前章」 | すべて **0件** |
| docs のリンク切れ | なし |

変換前のスナップショットはスクラッチパッドに退避済み（検証の基準）。

- **資料側の表記も変換済み**（用語集385件・図版TODO119件・章の骨30件＋見出し・全体プラン22件・校正TODO32件、パスと図名94件）
- **`build.py` を新設計で作り直し、`gen_index.py` を書き直した**（下記）

### ビルドの状態（動作確認済み・2026-08-14）

```
python3 gen_index.py                  → 資料/章一覧.md・資料/図版台帳.md
.venv/bin/python build.py --preview   → _preview/  全章（著者チェック用）
.venv/bin/python build.py             → docs/      approved のみ（公開用）
```

`.venv` は Pillow を入れた作業用の仮想環境（`.gitignore` 済み）。無ければ
`python3 -m venv .venv && .venv/bin/pip install -q Pillow` で作る。

| 実装したもの | |
|---|---|
| 入力 | `目次.yml`（section > part の2階層）＋ front matter。MANIFESTは廃止 |
| `[[id]]` | 章題＋リンクに解決。**未解決ならビルドを止める**。図のキャプションはリンクを張らず章題だけ開く |
| 公開ゲート | `docs/` は approved のみ／`--preview` は全章（statusバッジ付き） |
| ナビ・目次 | セクション > 部の2階層。部の扉に `目次.yml` の `intro` |
| ページ送り | 目次.yml から自動生成（未公開章は飛ばす） |
| 更新情報 | NEW/更新バッジ・章ページの日付・`changelog.html`・表紙の「最近の更新」 |
| 図 | `figures/*.png` → `<出力先>/images/*.webp`（Pillow必須） |

検証：リンク切れ0／未展開の `[[` 0／図 参照325・実体326・欠け0・未参照0。

### 次にやること

1. **著者チェック** ← いまここ。`_preview/index.html` を開いて読み、OKな章を `status: approved` にして `published:` と `history:` を入れる
   - **公開済み（7本）**：細胞とは何か／オルガネラ／オルガネラはどう連携するか／DNAからタンパク質まで／エピゲノム／受容体と細胞内シグナル／細胞膜と物質輸送
   - 公開済みの章は**ダブルチェック中**。表現の引っかかりを直しながら進めている
2. **図2点**が未作成 … `transdermal-delivery_越えさせ方の違い` / `_まとめ`（導入の章）
3. **腸内細菌**は制作メモだけ。骨と構成は本文中のコメントに残してある
4. `figures/`（ソースPNG）は当面 `.gitignore`。**図の差し替えが一段落したら1回で入れる**（旧版を履歴に残さないため）

**後回し**：主訴索引（front matter `適応:` ＋索引ページ）。あるとよいが優先度は高くない。分類軸を決めるのが先。

---

## 保留タスク

- **図が0点の章が1つある** … `抗酸化サプリメントと点滴`（旧第56章）。`資料/図版TODO.md` へ
- **文献の照合** … `資料/校正TODO.md`。確度が低いものに固有番号を付けない方針は維持
- **著者レビューの反映** … `資料/著者レビュー_2026-08.xlsx`
- **図版TODO** … `資料/図版TODO.md`
- **平易版（看護師版）** … `資料/平易版_仕様.md`。新サイトに含めるかは未決定

---

## 決めていないこと

2. 予約公開＋GitHub Actionsで自動化するか、毎週手動でビルド＆pushするか
4. 平易版を新サイトに含めるか
5. 主訴索引の分類軸（`適応:` に何を立てるか）

---

## 触らないもの

- `~/Desktop/黄先生宿題`（旧プロジェクト。凍結）
- `~/Desktop/orchestra`（旧と同内容の別フォルダ。無関係）
- iCloud
